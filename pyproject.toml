[project]
name = "MP3toSpotify"
version = "3.2.0"
description = "Match local music files & YouTube playlists to Spotify — GUI & CLI"
license = "GPL-3.0-only"
requires-python = ">=3.10"
dependencies = [
    "spotipy>=2.23",
    "tinytag>=1.9",
    "chardet>=5.0",
    "yt-dlp>=2024.1",
]
